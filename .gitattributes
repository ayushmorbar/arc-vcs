Cargo.lock linguist-generated=true merge=binary

# Keep shell and task runner files deterministic across platforms.
*.sh text eol=lf
justfile text eol=lf

# Keep config files text-normalized for reproducible diffs.
*.toml text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
