[package]
name = "arc-algebra-types"
version = "0.1.0-beta.6"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Foundational algebra types for arc"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
blake3 = { version = "1.8.4", features = ["serde"] }
