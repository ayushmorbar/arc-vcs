[package]
name = "arc-git-native"
version = "0.1.0-beta.6"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Zero-dependency in-memory JIT Git object synthesizer for arc"

[lib]
name = "arc_git_native"
path = "src/lib.rs"

[dependencies]
arc-core = { path = "../arc-core", default-features = false }
sha1 = "0.11.0"
