[package]
name = "arc-transaction"
version = "2026.7.19"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Pure transactional rewrite planning state for arc"

[dependencies]
arc-store-types = { path = "../arc-store-types" }
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.150"
bincode = "3.0.0"
